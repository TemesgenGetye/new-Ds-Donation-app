apiVersion: apps/v1
kind: Deployment
metadata:
  name: campaign-service
  namespace: donation-app
  labels:
    app: campaign-service
    service: campaign
spec:
  replicas: 2
  selector:
    matchLabels:
      app: campaign-service
  template:
    metadata:
      labels:
        app: campaign-service
        service: campaign
    spec:
      containers:
      - name: campaign-service
        image: finaldonationds-campaign-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3002
          name: http
        env:
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: NODE_ENV
        - name: PORT
          value: "3002"
        - name: CAMPAIGN_SUPABASE_URL
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: CAMPAIGN_SUPABASE_URL
        - name: CAMPAIGN_SUPABASE_KEY
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: CAMPAIGN_SUPABASE_KEY
        - name: SUPABASE_URL
          value: ""
        - name: SUPABASE_KEY
          value: ""
        - name: RABBITMQ_URL
          value: "amqp://guest:guest@rabbitmq:5672"
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: JWT_SECRET
        livenessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: campaign-service
  namespace: donation-app
  labels:
    app: campaign-service
    service: campaign
spec:
  type: NodePort
  ports:
  - port: 3002
    targetPort: 3002
    protocol: TCP
    nodePort: 30002
    name: http
  selector:
    app: campaign-service
