apiVersion: apps/v1
kind: Deployment
metadata:
  name: request-service
  namespace: donation-app
  labels:
    app: request-service
    service: request
spec:
  replicas: 2
  selector:
    matchLabels:
      app: request-service
  template:
    metadata:
      labels:
        app: request-service
        service: request
    spec:
      containers:
      - name: request-service
        image: finaldonationds-request-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3004
          name: http
        env:
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: NODE_ENV
        - name: PORT
          value: "3004"
        - name: REQUEST_SUPABASE_URL
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: REQUEST_SUPABASE_URL
        - name: REQUEST_SUPABASE_KEY
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: REQUEST_SUPABASE_KEY
        - name: SUPABASE_URL
          value: ""
        - name: SUPABASE_KEY
          value: ""
        - name: RABBITMQ_URL
          value: "amqp://guest:guest@rabbitmq:5672"
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: JWT_SECRET
        livenessProbe:
          httpGet:
            path: /health
            port: 3004
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3004
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: request-service
  namespace: donation-app
  labels:
    app: request-service
    service: request
spec:
  type: NodePort
  ports:
  - port: 3004
    targetPort: 3004
    protocol: TCP
    nodePort: 30004
    name: http
  selector:
    app: request-service
